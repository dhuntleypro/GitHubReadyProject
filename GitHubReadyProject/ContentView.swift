//
//  ContentView.swift
//  GitHubReadyProject
//
//  Created by Darrien Huntley on 12/29/20.
//

// Make sure Create Git Repository is not checked
import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Make sure Create Git Repository is not checked ")

            Text("preferances | + | GitHub | Signin")
            
            Text("Source Control | Create ReGit Repository")

            
            
            
        }
       
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
